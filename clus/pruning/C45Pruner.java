package clus.pruning;

import clus.main.*;

public class C45Pruner implements PruneTree {

	public void prune(ClusNode node) {
		if (!node.atBottomLevel()) {
/*			int arity = node.getArity();
			for (int i = 0; i < arity; i++) {
				prune(node.getChild(i));
			}
			double errLeaf = getEstimatedLeafError(node);
			double errTree = getEstimatedTreeError(node);
			if (errLeaf <= errTree*/

		}		
		
	}

}
